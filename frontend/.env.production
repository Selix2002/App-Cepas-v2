VITE_API_URL=/api
# IA (chat + feedback) desactivada hasta que IT actualice la CPU de la VM
# (numpy/torch exigen x86-64-v2 + AVX; la VM actual es qemu64 v1).
# Re-activar: poner VITE_IA_ENABLED=true (o quitar esta línea) y rebuildear.
VITE_IA_ENABLED=false