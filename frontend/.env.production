VITE_API_URL=/api
# IA (chat + feedback) activada vía Cohere (EMBEDDING_PROVIDER=cohere en el backend),
# sin depender del fix de CPU de la VM (numpy/torch no se cargan con ese provider).
VITE_IA_ENABLED=true