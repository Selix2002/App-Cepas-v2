[project]
name = "backend"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "litestar[standard]",
    "beanie", # ODM para MongoDB con soporte async
    "motor", # Driver async de MongoDB
    "pydantic-settings",
    "uvicorn",
    "bcrypt>=5.0.0",
    "redis>=7.2.0",
    "pyjwt>=2.11.0",
    "httpx>=0.28.1",
    "sentence-transformers>=5.2.3",
]
